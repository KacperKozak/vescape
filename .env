# Committed defaults. Public prod values only — secrets/local overrides go in
# .env.local (gitignored), which Expo layers on top of this file.

# Vescape backend (../vescape-server). Prod default; .env.local points it at a
# local `bun dev` server for development.
EXPO_PUBLIC_SERVER_URL=https://vescape.app
