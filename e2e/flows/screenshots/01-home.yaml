# Hero panel: connected home with the live replay filling the sparklines. The runner holds this one
# until the whole recentWindowMs sparkline window has elapsed at 1x — see scripts/screenshots.ts.
appId: ${APP_ID}
---
- runFlow: _home.yaml
- takeScreenshot: ${OUT_DIR}/01-home
