diff --git a/node_modules/@rnmapbox/maps/.bun-tag-85206b0b7d9270ff b/.bun-tag-85206b0b7d9270ff
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/android/src/main/java/com/rnmapbox/rnmbx/components/camera/RNMBXCameraModule.kt b/android/src/main/java/com/rnmapbox/rnmbx/components/camera/RNMBXCameraModule.kt
index 722903d70657a81d7f78bcff50c03ea424887b5b..b30db553c2049bb017d2f88f57e7acb995b4713b 100644
--- a/android/src/main/java/com/rnmapbox/rnmbx/components/camera/RNMBXCameraModule.kt
+++ b/android/src/main/java/com/rnmapbox/rnmbx/components/camera/RNMBXCameraModule.kt
@@ -8,6 +8,8 @@ import com.facebook.react.bridge.ReadableMap
 import com.facebook.react.bridge.WritableMap
 import com.facebook.react.bridge.WritableNativeMap
 import com.mapbox.maps.ScreenCoordinate
+import com.mapbox.geojson.Point
+import com.mapbox.maps.CameraOptions
 import com.mapbox.maps.plugin.animation.MapAnimationOptions
 import com.mapbox.maps.plugin.animation.easeTo
 import com.mapbox.maps.plugin.animation.moveBy
@@ -57,6 +59,22 @@ class RNMBXCameraModule(context: ReactApplicationContext, val viewTagResolver: V
         }
     }
 
+    override fun setCameraDirect(viewRef: ViewRefTag?, pitch: Double, heading: Double, centerLongitude: Double, centerLatitude: Double, promise: Promise) {
+        withViewportOnUIThread(viewRef, promise) {
+            val options = CameraOptions.Builder()
+                .apply {
+                    if (!pitch.isNaN()) pitch(pitch)
+                    if (!heading.isNaN()) bearing(heading)
+                    if (!centerLongitude.isNaN() && !centerLatitude.isNaN()) {
+                        center(Point.fromLngLat(centerLongitude, centerLatitude))
+                    }
+                }
+                .build()
+            it.mapboxMap?.setCamera(options)
+            promise.resolve(null)
+        }
+    }
+
     private fun getAnimationOptions(
         animationMode: Double,
         animationDuration: Double
diff --git a/ios/RNMBX/RNMBXCamera.swift b/ios/RNMBX/RNMBXCamera.swift
index bc3f6321a79d21ed1294b2e23c6d661dd1b8f50d..fff8fa80e84835fd88d8fad681a358a3c5d9de09 100644
--- a/ios/RNMBX/RNMBXCamera.swift
+++ b/ios/RNMBX/RNMBXCamera.swift
@@ -313,6 +313,19 @@ open class RNMBXCamera : RNMBXMapAndMapViewComponentBase {
   @objc public func updateCameraStop(_ stop: [String: Any]) {
     self.stop = stop
   }
+
+  @objc public func setCameraDirect(_ pitch: Double, heading: Double, centerLongitude: Double, centerLatitude: Double) {
+    withMapView { map in
+      let center = (centerLongitude.isNaN || centerLatitude.isNaN)
+        ? nil
+        : CLLocationCoordinate2D(latitude: centerLatitude, longitude: centerLongitude)
+      map.mapboxMap.setCamera(to: CameraOptions(
+        center: center,
+        bearing: heading.isNaN ? nil : heading,
+        pitch: pitch.isNaN ? nil : pitch
+      ))
+    }
+  }
   
   func _toCoordinateBounds(_ bounds: FeatureCollection) throws -> CoordinateBounds  {
     guard bounds.features.count == 2 else {
diff --git a/ios/RNMBX/RNMBXCameraModule.mm b/ios/RNMBX/RNMBXCameraModule.mm
index 6599a79f947b737709bf9bab13dfc497231eb8e2..1ccf675bb29b8d57c38daae04219e17ab1568af9 100644
--- a/ios/RNMBX/RNMBXCameraModule.mm
+++ b/ios/RNMBX/RNMBXCameraModule.mm
@@ -50,6 +50,20 @@ - (void)withCamera:(nonnull NSNumber*)viewRef block:(void (^)(RNMBXCamera *))blo
     } reject:reject methodName:@"updateCameraStop"];
 }
 
+RCT_EXPORT_METHOD(setCameraDirect:(nonnull NSNumber *)viewRef
+                  pitch:(double)pitch
+                  heading:(double)heading
+                  centerLongitude:(double)centerLongitude
+                  centerLatitude:(double)centerLatitude
+                  resolve:(RCTPromiseResolveBlock)resolve
+                  reject:(RCTPromiseRejectBlock)reject)
+{
+    [self withCamera:viewRef block:^(RNMBXCamera *view) {
+        [view setCameraDirect:pitch heading:heading centerLongitude:centerLongitude centerLatitude:centerLatitude];
+        resolve(@true);
+    } reject:reject methodName:@"setCameraDirect"];
+}
+
 RCT_EXPORT_METHOD(moveBy:(nonnull NSNumber *)viewRef
                   x:(double)x
                   y:(double)y
diff --git a/lib/module/components/Camera.js b/lib/module/components/Camera.js
index da6b273215f41d316fdb1d638ede2de5c73384a1..bdaebbd641ffacdaee4490967c9a98124437cf2e 100644
--- a/lib/module/components/Camera.js
+++ b/lib/module/components/Camera.js
@@ -201,6 +201,9 @@ export const Camera = /*#__PURE__*/memo(/*#__PURE__*/forwardRef((props, ref) =>
     }
   };
   const setCamera = useCallback(_setCamera, [allowUpdates, buildNativeStop, commands]);
+  const setCameraDirect = useCallback(config => {
+    commands.call('setCameraDirect', [config.pitch ?? Number.NaN, config.heading ?? Number.NaN, config.centerCoordinate?.[0] ?? Number.NaN, config.centerCoordinate?.[1] ?? Number.NaN]);
+  }, [commands]);
   const _fitBounds = (ne, sw, paddingConfig = 0, _animationDuration = 0) => {
     let _padding = {
       paddingTop: 0,
@@ -288,6 +291,7 @@ export const Camera = /*#__PURE__*/memo(/*#__PURE__*/forwardRef((props, ref) =>
      * @param {CameraStop | CameraStops} config
      */
     setCamera,
+    setCameraDirect,
     /**
      * Set the camera position to enclose the provided bounds, with optional
      * padding and duration.
diff --git a/lib/typescript/src/components/Camera.d.ts b/lib/typescript/src/components/Camera.d.ts
index 5ba484344d14d55f17afa8dd11f2bc02904f8aaf..d601b3cbcb09f6ddc5b2d4dffd263552d2b40d89 100644
--- a/lib/typescript/src/components/Camera.d.ts
+++ b/lib/typescript/src/components/Camera.d.ts
@@ -12,6 +12,11 @@ export type UserTrackingModeChangeCallback = (event: MapboxGLEvent<'usertracking
 }>) => void;
 export interface CameraRef {
     setCamera: (config: CameraStop | CameraStops) => void;
+    setCameraDirect: (config: {
+        pitch?: number;
+        heading?: number;
+        centerCoordinate?: Position;
+    }) => void;
     fitBounds: (ne: Position, sw: Position, paddingConfig?: number | number[], animationDuration?: number) => void;
     flyTo: (centerCoordinate: Position, animationDuration?: number) => void;
     moveTo: (centerCoordinate: Position, animationDuration?: number) => void;
diff --git a/lib/typescript/src/specs/NativeRNMBXCameraModule.d.ts b/lib/typescript/src/specs/NativeRNMBXCameraModule.d.ts
index 3fda8aa42ab2d96329be85791bee4dd0f8141557..4e7e6179c4d9ac44b6fb02bcd4adffb8c99bf88c 100644
--- a/lib/typescript/src/specs/NativeRNMBXCameraModule.d.ts
+++ b/lib/typescript/src/specs/NativeRNMBXCameraModule.d.ts
@@ -20,6 +20,7 @@ type Stop = {
 type ObjectOr<_T> = Object;
 export interface Spec extends TurboModule {
     updateCameraStop(viewRef: ViewRef, stop: ObjectOr<Stop>): Promise<void>;
+    setCameraDirect(viewRef: ViewRef, pitch: number, heading: number, centerLongitude: number, centerLatitude: number): Promise<void>;
     moveBy: (viewRef: ViewRef, x: number, y: number, animationMode: number, animationDuration: number) => Promise<void>;
     scaleBy: (viewRef: ViewRef, x: number, y: number, animationMode: number, animationDuration: number, scaleFactor: number) => Promise<void>;
 }
diff --git a/src/components/Camera.tsx b/src/components/Camera.tsx
index c8e61a0d9e528bf74e869e14153e9a5be5894be7..79ddc19467de1d99bb125191468a73c584a41d4b 100644
--- a/src/components/Camera.tsx
+++ b/src/components/Camera.tsx
@@ -97,6 +97,11 @@ interface NativeCameraStop {
 
 export interface CameraRef {
   setCamera: (config: CameraStop | CameraStops) => void;
+  setCameraDirect: (config: {
+    pitch?: number;
+    heading?: number;
+    centerCoordinate?: Position;
+  }) => void;
   fitBounds: (
     ne: Position,
     sw: Position,
@@ -473,6 +478,22 @@ export const Camera = memo(
         commands,
       ]);
 
+      const setCameraDirect = useCallback(
+        (config: {
+          pitch?: number;
+          heading?: number;
+          centerCoordinate?: Position;
+        }) => {
+          commands.call<void>('setCameraDirect', [
+            config.pitch ?? Number.NaN,
+            config.heading ?? Number.NaN,
+            config.centerCoordinate?.[0] ?? Number.NaN,
+            config.centerCoordinate?.[1] ?? Number.NaN,
+          ]);
+        },
+        [commands],
+      );
+
       const _fitBounds: CameraRef['fitBounds'] = (
         ne,
         sw,
@@ -605,6 +626,7 @@ export const Camera = memo(
          * @param {CameraStop | CameraStops} config
          */
         setCamera,
+        setCameraDirect,
         /**
          * Set the camera position to enclose the provided bounds, with optional
          * padding and duration.
diff --git a/src/specs/NativeRNMBXCameraModule.ts b/src/specs/NativeRNMBXCameraModule.ts
index 51e22647de0011459dee4bd160ede4fcca1f4263..a5820854ae329bcee9328089324bad87b63eaa48 100644
--- a/src/specs/NativeRNMBXCameraModule.ts
+++ b/src/specs/NativeRNMBXCameraModule.ts
@@ -30,6 +30,7 @@ type ObjectOr<_T> = Object;
 
 export interface Spec extends TurboModule {
   updateCameraStop(viewRef: ViewRef, stop: ObjectOr<Stop>): Promise<void>;
+  setCameraDirect(viewRef: ViewRef, pitch: number, heading: number, centerLongitude: number, centerLatitude: number): Promise<void>;
   moveBy: (
     viewRef: ViewRef,
     x: number,
