/**
 * Bundled default shown when the server resolves an Update Warning but the matched rule carries no
 * message. Markdown, rendered by `Markdown`. The server can override this per release.
 */
export const DEFAULT_UPDATE_WARNING_MESSAGE =
  'A newer version of Vescape is available. Updating soon keeps you compatible with online features and current fixes.'
